# secrets & local config
.env
.env.*
!.env.example
*.key
secrets/
profile.yaml

# vendored third-party tools (cloned separately, not our code — see README setup step)
vendor/

# python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# node
node_modules/
dist/
build/
*.log
.npm/

# downloaded reel media (never commit reel audio/video)
media/
downloads/
*.mp4
*.m4a
*.mp3
*.wav

# local db
*.sqlite
*.sqlite3
*.db

# images / screenshots
*.jpg
*.jpeg

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# local editor/tooling config — machine-specific, not part of this project
.agents/
AGENTS.md
.claude/
# local tooling scratch: carries a full node_modules and machine-local config, not project source.
# This repo is PUBLIC, so leaving it untracked is one `git add .` away from publishing it.
.opencode/

# local server logs
.server.log
.server.err.log


# local build/process write-ups — working notes, not project source
reports/

# generated codebase-analysis artifacts (regenerable, go stale) — not project source
.ua/
