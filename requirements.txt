# v1 deps grow per task (incremental). Task 1: web skeleton + tests + lint.
fastapi
uvicorn[standard]
httpx          # FastAPI TestClient dependency
pytest
ruff
sqlmodel       # Task 2: Reel model + store (pulls SQLAlchemy + pydantic)
jinja2         # Task 3: dashboard templates
yt-dlp         # Task 4: download a public reel's audio
faster-whisper # Task 5: local transcription (no API key)
openai         # Task 6: NVIDIA NIM via the OpenAI-compatible API
pyyaml         # Task 6: load the focus profile
python-dotenv  # load .env in dev
feedparser     # tech-radar source #4: parse RSS news / lab-blog feeds
python-multipart  # parse the save/unsave form posts (FastAPI Form)
